# Application Configuration
spring.application.name=worker-connect-backend

# Database - Supabase PostgreSQL
spring.datasource.url=jdbc:postgresql://db.hsqcivtwxrwzlpwymmot.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=Sujipraveen@2724
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT
app.jwt.secret=WorkerConnect2024SuperSecretKeyMustBeAtLeast256BitsLong!SecureKey
app.jwt.expiration=86400000

# Server
server.port=8080

# CORS (will be refined in config)
spring.mvc.cors.allowed-origins=*

# Multipart file upload (we handle via Supabase JS directly)
spring.servlet.multipart.enabled=false

# Logging
logging.level.com.workerconnect=DEBUG
logging.level.org.springframework.security=DEBUG
